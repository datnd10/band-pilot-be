spring.application.name=band-pilot

# ─── Active profile ───────────────────────────────────────────────────────────────
# docker: PostgreSQL via docker-compose (local development)
# prod  : PostgreSQL via environment variables (CI / cloud deployment)
spring.profiles.active=docker
